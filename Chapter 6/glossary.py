glossary = {
    "for": "Python's way of a loop",
    "if": "Conditional statement",
    "else": "If 'if' does not match, then do the else statement",
    "shuffle": "Choose a random item on the list or dictionary",
    "import": "Imports additional features",
    "items": "Access to both key and the values",
    "keys": "Access to all keys only",
    "values": "Access to all values only",
    "sorted": "Sorts elements in a chronological order",
    "set": "Includes only unique elements by removing the duplicates"
}

for key, value in glossary.items():
    print(f"'{key}' has the definition: '{value}'.")